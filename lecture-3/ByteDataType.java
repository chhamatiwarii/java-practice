class ByteDataType {
    
    public static void main(String[] args) {
        byte b = 10;
        // byte b2 = 130; // Chhote taale me badi chabhi
        // byte b2 = 10.5;  // incompatible types
        // byte b3 = true;  // incompatible types
        // byte b4 = "chhama";  // incompatible types

        //  Byte data type is most suitable if we are handling
        // data in terms of streams either from file or from the network.
        System.out.println(Byte.MAX_VALUE);
        System.out.println(Byte.MIN_VALUE);
    }
}
